/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove webpackDevMiddleware configuration
  reactStrictMode: true,
  // Add any other necessary configurations here
}

module.exports = nextConfig